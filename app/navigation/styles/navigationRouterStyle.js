// @flow
import {StyleSheet} from 'react-native';
import {MainStyles} from '../../theme/';

export default StyleSheet.create({
  navBar: {
    ...MainStyles.navBar
  },
  tabBar: {
    ...MainStyles.tabBar
  }
});
